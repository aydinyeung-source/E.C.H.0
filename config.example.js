// Template for config.js — copy your real values into config.js (which is the
// file the game actually loads). For a static site with no build step, config.js
// must be committed so Cloudflare Pages can serve it.
//
// The anon key is a PUBLIC key. Keep it safe by enabling Row Level Security —
// run supabase/leaderboard.sql before going live.
window.SUPABASE_URL = "https://YOUR-PROJECT.supabase.co";
window.SUPABASE_ANON_KEY = "YOUR-PUBLIC-ANON-KEY";
