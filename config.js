// Supabase configuration, served directly to the browser.
//
// Leave these BLANK to use the built-in offline (localStorage) leaderboard.
//
// To enable the GLOBAL leaderboard:
//   1. Create a Supabase project and run supabase/leaderboard.sql in its
//      SQL editor (this also turns on Row Level Security).
//   2. Paste your project URL and PUBLIC anon key below.
//   3. Commit & push — Cloudflare Pages redeploys automatically.
//
// The anon key is designed to be public and is safe to commit *as long as*
// Row Level Security is enabled (the SQL file sets that up).
window.SUPABASE_URL = "";
window.SUPABASE_ANON_KEY = "";
